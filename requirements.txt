# Aestra / CycleBench — pinned Python dependencies (reproducibility)
# Install: python3 -m venv .venv && source .venv/bin/activate && make install
#
# Core offline path needs everything below except optional extras at the bottom.

pydantic==2.13.4
pandas==3.0.3
numpy==2.5.1
scipy>=1.14,<2
scikit-learn>=1.5,<2
joblib>=1.3,<2

fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
httpx>=0.27,<1
python-dotenv>=1.0,<2

pytest>=8,<9

# Optional LLM rephrase / extract (demo runs without a key)
openai>=1.40,<2

# Optional: only for scripts/download_kaggle.py
# kaggle>=1.6,<2

# Optional: Personalized Federated Learning research (federated_learning/)
# Install with: make install-pfl
# torch>=2.2
# matplotlib>=3.8
